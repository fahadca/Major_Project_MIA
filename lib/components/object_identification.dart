import 'package:flutter/material.dart';

class ObjectIdentificationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Object Identification')),
      body: Center(child: Text('Object Identification Page')),
    );
  }
}